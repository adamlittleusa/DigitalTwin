# apps/web

The adambuilds.ai site: Next.js (App Router), TypeScript, hand-written CSS, no UI library.

Run locally with `npm run dev`.

Content lives in `content/`.

See `docs/superpowers/specs/2026-09-06-site-design.md` for the full design.
